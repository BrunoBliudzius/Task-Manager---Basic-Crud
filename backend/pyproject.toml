[project]
name = "backend"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [
    { name = "Bruno", email = "brunosilvabliu@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "psycopg2>=2.9.13",
]

[project.scripts]
backend = "backend:main"

[tool.fastapi]
entrypoint = "src.backend.__init__:app"   

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
